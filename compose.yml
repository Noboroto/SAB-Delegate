services:
  bot:
    build: .
    restart: always
    volumes:
      - ./root:/usr/src/bot
      - ./root/files:/usr/src/bot/files
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TOKENS=
